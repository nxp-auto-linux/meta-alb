#@TYPE: Machine
#@NAME: NXP S32V234EVB
#@SOC: s32v2xx
#@DESCRIPTION: Machine configuration for running the S32V234EVB in 64-bit mode
#@MAINTAINER: Catalin Udma <catalin-dan.udma@nxp.com>

require conf/machine/include/s32-base.inc

UBOOT_SUFFIX ?= "s32"
# not sure if really needed - to check
UBOOT_ENTRYPOINT ?= "80080000"

KERNEL_DEVICETREE ?= "freescale/s32v234-evb.dtb"
KERNEL_DEFCONFIG ?= "${S}/arch/arm64/configs/s32v234_defconfig"

SERIAL_CONSOLE = "115200 ttyLF0"

# not sure if really needed - to check
EXTRA_IMAGEDEPENDS += "u-boot-s32"

# this is rather in include/.inc file
#IMAGE_FSTYPES += "ext3 sdcard"
#SDCARD_ROOTFS ?= "${DEPLOY_DIR_IMAGE}/${IMAGE_NAME}.rootfs.ext3"
#IMAGE_FSTYPES_remove = "jffs2 ubi ext2.gz ext2.gz.u-boot"
